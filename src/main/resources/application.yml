spring:
  application:
    name: Oauth2
  security:
    oauth2:
      client:
        registration:
          github:
            client-id:
            client-secret:
          google:
            client-id:
            client-secret:

logging:
  level:
    org.springframework.security: DEBUG



